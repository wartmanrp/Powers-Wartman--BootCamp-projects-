using System;
using System.IO;
using System.Web;
using Cts.Venture.Domain;
using Cts.Venture.Service;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Cts.Venture.Tests.Services
{
   [TestClass]
   public class AuthServiceTest
   {
      //[TestMethod]
      //public void Login()
      //{
      //   HttpContext.Current = new HttpContext(new HttpRequest("", "http://tempuri.org", ""), new HttpResponse(new StringWriter()));
      //   User model = new User { Username = "hmunikar", Password = "password" };

      //   UserService service = new UserService();
      //   bool result = service.Login(model);

      //   Assert.IsTrue(result);
      //}

      //[TestMethod]
      //public void Login_Get()
      //{
      //   HttpContext.Current = new HttpContext(new HttpRequest("", "http://tempuri.org", ""), new HttpResponse(new StringWriter()));
      //   User model = new User { Username = "hmunikar", Password = "password" };

      //   UserService service = new UserService();
      //   bool result = service.Login(model);

      //   Assert.IsTrue(result);
      //}

      //[TestMethod]
      //public void Password()
      //{
      //   string hash = Cts.Venture.Common.Helpers.HashHelper.GetHash("password", Cts.Venture.Common.Helpers.HashAlgorithms.SHA512, null);

      //}


   }
}
